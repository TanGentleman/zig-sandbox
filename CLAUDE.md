# tracers

Zig 0.16 monorepo. `tracers/` is the shipping CLI; `sandbox/` is throwaway experiments.

## Zig docs

Use `zigpeek` for stdlib and builtin lookups. If it's not installed:

```sh
uv tool install 'zigpeek[offline]'
```
