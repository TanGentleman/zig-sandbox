# tracers

Drive [looptap](https://github.com/TanGentleman/looptap) over `~/.claude/projects` and surface the rough sessions — see [`tracers/`](tracers/) for the shipping CLI. `sandbox/` is throwaway Zig 0.16 experiments.
